{
  "name": "@botc/sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CLI simulator for balance checking. Runs N games with different AI brains, produces winrate report.",
  "scripts": {
    "start": "bun run src/cli.ts",
    "dev": "bun --watch run src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@botc/core": "workspace:*"
  }
}
